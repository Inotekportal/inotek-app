export const metadata = {
  title: 'Mój Pierwszy Prawdziwy Post',
  description:
    'To jest mój pierwszy post w MDX. Używamy obiektu metadata zamiast YAML frontmatter.',
  date: '2025-11-17',
};

# To jest mój pierwszy post w MDX

To jest treść ładowana z folderu `content/posts`!

Używamy `export const metadata` zamiast `---`.
